
# Tuples are like strings, you can't change the value. You can define it with parenthesis inside of a Variable.

mi_tuple = (1,2, (10,20),4)

print(mi_tuple[2][0])



tuple = (1,2,3,1)

# Prints how many times the value you assign, it's repeated (inside the tuple).
print(tuple.count(1))


# Prints the index value of a Tuple

print(tuple.index(2))